# API Homework Exercise

Python script that fetches weather data from a protected API,
handles paginating, retries and token expiration, calculates the average temperature
and prints the city name and average temperature.